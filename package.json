{
  "name": "neuro-nav",
  "version": "1.0.0",
  "private": true,
  "description": "The Developer's Micro-OS — A browser-integrated micro operating system for software engineers",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/extension",
    "build": "npm run build -w apps/extension",
    "build:server": "npm run build -w packages/nav-server",
    "build:cli": "npm run build -w packages/nav-cli",
    "build:all": "npm run build && npm run build:server && npm run build:cli",
    "package:extension": "npm run build && cd apps/extension && zip -r ../../neuro-nav-extension.zip dist/ -x '*.map'",
    "test": "npm run test -w apps/extension",
    "lint": "npm run lint -w apps/extension"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
